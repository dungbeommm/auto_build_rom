{
  "stock": "",
  "port": "",
  "pack_type": "super",
  "fs_type": "erofs",
  "features": [
    {
      "name": "cn_global_patch",
      "required": false,
      "options": {"rule_file": "patches/rom_cn_patch.json"}
    },
    {
      "name": "keyboard_unlock",
      "required": false,
      "options": {"rule_file": "patches/keyboard_unlock.json"}
    },
    {
      "name": "add_apps",
      "required": false,
      "options": {"source_dir": "assets/apps", "dest": "product/app"}
    },
    {
      "name": "app_patches",
      "required": false,
      "options": {"rule_file": "patches/app_patches.json"}
    },
    {
      "name": "boot_patch",
      "required": false,
      "options": {"image": "boot.img", "overlay_dir": "patches/boot"}
    },
    {
      "name": "vendor_boot_patch",
      "required": false,
      "options": {"image": "vendor_boot.img", "overlay_dir": "patches/vendor_boot"}
    }
  ]
}
