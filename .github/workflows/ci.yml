name: CI
on: [push,pull_request]
permissions: {contents: read}
jobs:
 test:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v4
   - run: bash -n scripts/*.sh && python3 -m py_compile scripts/*.py tests/*.py && python3 -m unittest discover -s tests -v
